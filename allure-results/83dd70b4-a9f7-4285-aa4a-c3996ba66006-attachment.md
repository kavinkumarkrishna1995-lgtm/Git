# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: amazon.spec.js >> amazon order
- Location: tests\amazon.spec.js:4:5

# Error details

```
Error: locator.click: Test ended.
Call log:
  - waiting for getByRole('button', { name: /sign in with an otp/i })

```

# Test source

```ts
  1  | import { test } from '@playwright/test';
  2  | import { createInterface } from 'node:readline';
  3  | 
  4  | test('amazon order', async ({ page }) => {
  5  |   // Open Amazon and go to Sign in
  6  |   await page.goto('https://www.amazon.in/', { waitUntil: 'domcontentloaded' });
  7  |   await page.getByRole('link', { name: /hello, sign in/i }).click();
  8  | 
  9  |   // Enter mobile number and click Continue
  10 |   await page.getByLabel(/mobile number/i).fill('9361537681');
  11 |   await page.getByRole('button', { name: /continue/i }).click({ timeout: 10000 });
  12 |   await page.waitForTimeout(2000);
  13 | 
  14 |   // Native passkey window appears - manually click Cancel, then resume
  15 |   await page.pause();
  16 | 
> 17 |   // Select Sign in with an OTP
     |                                                                    ^ Error: locator.click: Test ended.
  18 |   await page.getByText(/sign in with an otp/i).click();
  19 | 
  20 |   // Enter OTP manually in terminal
  21 |   const rl = createInterface({ input: process.stdin, output: process.stdout });
  22 |   const otp = await new Promise(resolve => rl.question('Enter OTP: ', resolve));
  23 |   rl.close();
  24 | 
  25 |   // Enter OTP and verify
  26 |   await page.getByLabel(/otp/i).fill(otp);
  27 |   await page.getByRole('button', { name: /verify/i }).click();
  28 | 
  29 |   // Search for product
  30 |   await page.getByPlaceholder(/search amazon/i).fill('bump on board car sign board');
  31 |   await page.keyboard.press('Enter');
  32 | 
  33 |   // Open first search result
  34 |   await page.locator('[data-component-type="s-search-result"]').first()
  35 |     .getByRole('link').first().click();
  36 | 
  37 |   // Select quantity 1 and Buy Now
  38 |   await page.getByLabel(/quantity/i).selectOption('1');
  39 |   await page.getByRole('button', { name: /buy now/i }).click();
  40 | 
  41 |   // Stop before payment for manual QR scanning
  42 |   await page.pause();
  43 | });
  44 | 
```